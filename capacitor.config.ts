import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.keervit.food',
  appName: 'keervit',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
